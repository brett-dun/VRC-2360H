
void auto2() {

	startTask(pidTask);
	setDRFBAngle(10);

	driveInches(24);

	//move chain bar

	//intakeCone(1000, true); //Drop cone onto stationary goal

	//driveInches(-12);

	/*
	if(leftStart)
		turnDegrees(90);
	else
		turnDegrees(-90);
	*/

	//driveInches(12);

	/*
	if(leftStart)
		turnDegrees(-45);
	else
		turnDegrees(45);
	*/

	//driveInches(45);

	//raiseMobileGoal(); //pickup mobile goal

}
